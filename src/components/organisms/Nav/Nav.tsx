export interface NavProps {
    title?: string;
    maxCnt?: number;
    type?: string;
    subtitle?: string;
    essential?: boolean;
}
export function Nav ( {title, maxCnt, type, subtitle, essential}: NavProps ) {
    
    return (
        <>Nav</>
    );
};
